library_management_system/
│
├── main.py                  # Главный файл для запуска приложения
├── models/                  # Модели данных
│   ├── book.py              # Класс для книги
│   ├── librarian.py         # Класс для библиотекаря
│   └── reader.py            # Класс для читателя
│
├── services/                # Логика взаимодействия с моделями
│   ├── book_service.py      # Сервис для работы с книгами
│   ├── librarian_service.py # Сервис для работы с библиотекарем
│   └── reader_service.py    # Сервис для работы с читателями
│
│
├── utils/                   # Утилиты для вспомогательных функций
│   ├── file_manager.py      # Управление файлами (сохранение, загрузка)
│   ├── logger.py            # Логирование
│   └── search.py            # Поиск по сущностям
│
├── requirements.txt         # Зависимости проекта
└── README.md                # Описание проекта



Паттерны:










ответь на конкретно поставленный вопрос - нужно создать репозиторий для приложения который будет иметь классы - библиотека, библиотекарь, книга, читатель. Нужна понять как будет построена структура директорий, файлов.

Классы (сущности) будут создаваться фабричным методом
так же для поиска будет
дальше реализация